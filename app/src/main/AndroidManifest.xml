<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.omkar.ciboclient">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".FirstPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Login" />
        <activity android:name=".ForgotPassword" />
        <activity android:name=".SignUp" />
        <activity android:name=".MainScreen" />
        <activity android:name=".BookTable" />
        <activity android:name=".Location" />
        <activity android:name=".Menu" />
        <activity android:name=".TakeAway" />
        <activity android:name=".Offers" />
        <activity android:name=".MenuItemActivity" />
        <activity android:name=".TakeAwayMenuItem" />
        <activity android:name=".DineInActivity" />
        <activity android:name=".TakeAwayOrderView" />
        <activity android:name=".deliveryaddress" />
        <activity android:name=".ordersummary" />
        <activity android:name=".BookTableClient" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".maplocation"
            android:label="@string/title_activity_maplocation" />
        <activity android:name=".ViewHistoryActivity" />
        <activity android:name=".ViewDetailedHistoryActivity"></activity>
    </application>

</manifest>